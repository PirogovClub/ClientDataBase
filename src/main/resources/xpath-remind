јбсолютный и относительный XPaths (/ vs .)

/ вводит путь абсолютного местоположени€, начина€ с корн€ документа.
. вводит относительный путь местоположени€, начина€ с контекста node.
»менованный элемент vs любого элемента (ename vs *)

/ename выбирает корневой элемент ename
./ename выбирает все дочерние элементы ename текущего node.
/* выбирает корневой элемент независимо от имени.
./* или * выбирает все дочерние элементы контекста node, независимо от имени.
ось потомка-самца (//*)

//ename выбирает все элементы ename в документе.
.//ename выбирает все элементы ename в контексте или ниже контекста node.
//* выбирает все элементы документа независимо от имени.
.//* выбирает все элементы, независимо от имени, в контексте или ниже контекста node.
— учетом этих пон€тий, вот ответы на ваши конкретные вопросы...

.//*[@id='Passwd'] означает выбор всех элементов на уровне или ниже текущий контекст node, который имеет значение атрибута id, равное 'Passwd'.
//child::input[@type='password'] можно упростить до //input[@type='password'] и означает выбор всех элементов input в документе с атрибутом type, равным 'password'.